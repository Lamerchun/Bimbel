# Screenshots

App and mock screenshots go here. Name files clearly, e.g. `chat-thread-light.png`.

Leading inbox swipe (Read + Pin, mid-reveal): run `BimbelSampleUITests/testLeadingMidSwipeOnAda` from the `BimbelSample` scheme. It launches `BIMBEL_SHOT=inbox`, drags Ada LTR, and keeps an XCResult attachment named `slice2-qa-01-inbox-swipe-leading`.
