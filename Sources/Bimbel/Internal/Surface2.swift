import Foundation

// Surface 2 (conversation list / inbox) is intentionally not built in this cut.
// When it lands, it must consume `ConversationTheme` tokens from this module and
// the same drop-in rules as Surface 1. Do not introduce a parallel palette.
